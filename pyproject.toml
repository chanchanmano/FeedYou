[tool.poetry]
name = "foodline"
version = "0.1.0"
description = ""
authors = ["Ary"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
django = "^5.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
